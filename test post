<!doctype html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <title>{{ title }}</title>
    <link rel="stylesheet" href="stylesheets/styles.css">
</head>
<body>
    <header class="site-header">
        <a class="brand" href="/" aria-label="sh0ds@devblog">sh0ds<span>@devblog</span></a>
        <nav aria-label="Main">
            <ul>
                <li><a href="/blog.html">Blog</a></li>
                <li><a href="/projects.html">Projects</a></li>
            </ul>
        </nav>
    </header>

    <main>
<h3>header 3</h3>
<p>paragraph 3
</p>
<h4>header 4</h4>
<p>paragraph 4
</p>
<pre><code>int render_page(const char *temp_path, const char *md_path,
                const char *html_path) {
  int result = -1;
  char *temp = NULL, *pos = NULL;
  FILE *md = NULL;
  FILE *html = NULL;

  // read template into a string
  if ((temp = read_file(temp_path)) == NULL)
    goto cleanup;

  // find the position of the placeholder
  if ((pos = strstr(temp, &quot;{{ content }}&quot;)) == NULL) {
    fprintf(stderr, &quot;Cannot find placeholder in %s\n&quot;, temp_path);
    goto cleanup;
  }

  // open files
  md = fopen(md_path, &quot;rb&quot;);
  if (md == NULL) {
    perror(&quot;Error opening .md file&quot;);
    goto cleanup;
  }
  html = fopen(html_path, &quot;wb&quot;);
  if (html == NULL) {
    perror(&quot;Error opening .html file&quot;);
    goto cleanup;
  }

  // write upper half of template
  fwrite(temp, 1, pos - temp, html);

  // call md_convert for directly converting the content
  if (md_convert(md, html) == -1)
    goto cleanup;

  // write lower half of template
  fputs(pos + strlen(&quot;{{ content }}&quot;), html);

  result = 0;

cleanup:
  if (md != NULL)
    fclose(md);
  if (html != NULL) {
    if (fclose(html) != 0) {
      perror(html_path);
      result = -1;
    }
  }
  free(temp);

  return result;
}
</code></pre>

    </main>

    <footer class="site-footer">
        <p><small>© 2026 sh0ds. Hosted on RPi 5, built with <a href="https://github.com/sh0ds/3sgc" target="_blank">3sgc</a></small></p>
        <ul>
            <li><a href="https://github.com/sh0ds" target="_blank">GitHub</a></li>
            <li><a href="https://sh0ds.itch.io/" target="_blank">itch.io</a></li>
            <li><a href="mailto:mail@sh0ds.dev">Contact</a></li>
        </ul>
    </footer>

    <script src="https://cdnjs.cloudflare.com/ajax/libs/highlight.js/11.9.0/highlight.min.js"></script>
    <script>
        hljs.highlightAll();

        // brand hover: a band of 0s and 1s runs through sh0ds@devblog
        const brand = document.querySelector(".brand");
        const user = brand.firstChild;
        const host = brand.querySelector("span");
        const original = user.data + host.textContent;
        let frame;

        function bitflip() {
            if (matchMedia("(prefers-reduced-motion: reduce)").matches) return;
            cancelAnimationFrame(frame);
            const start = performance.now();
            let last = 0;

            (function tick(now) {
                const p = Math.min((now - start) / 650, 1);
                if (now - last > 30 || p === 1) {
                    last = now;
                    const band = p * (original.length + 6) - 3;
                    const text = [...original].map((c, i) =>
                        p < 1 && Math.abs(i - band) < 2.5 ? (Math.random() < .5 ? "0" : "1") : c
                    ).join("");
                    user.data = text.slice(0, user.data.length);
                    host.textContent = text.slice(user.data.length);
                }
                if (p < 1) frame = requestAnimationFrame(tick);
            })(start);
        }

        brand.addEventListener("mouseenter", bitflip);
        brand.addEventListener("focus", bitflip);
    </script>
</body>
</html>
